import styled from 'styled-components';

const Container = styled.div`
    padding: 1rem;
`;

const Title = styled.h2`
    font-size: 1.5rem;
    font-weight: bold;
`;

export const CategoryLists = () => {

    return (
        <Container>
            <Title>Hello World - Category Lists</Title>
        </Container>
    );
};
